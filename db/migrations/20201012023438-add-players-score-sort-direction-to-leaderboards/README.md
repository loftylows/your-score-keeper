# Migration `20201012023438-add-players-score-sort-direction-to-leaderboards`

This migration has been generated by Damien Gresham at 10/12/2020, 9:34:38 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "public"."LeaderboardPlayersScoreSortDirection" AS ENUM ('ASC', 'DESC')

ALTER TABLE "public"."Leaderboard" ADD COLUMN "playersScoreSortDirection" "LeaderboardPlayersScoreSortDirection"  NOT NULL DEFAULT E'DESC'
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201004050420-initial-migration..20201012023438-add-players-score-sort-direction-to-leaderboards
--- datamodel.dml
+++ datamodel.dml
@@ -2,17 +2,22 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgres"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
 // --------------------------------------
+enum LeaderboardPlayersScoreSortDirection {
+  ASC
+  DESC
+}
+
 model User {
   id                 String        @id @default(uuid())
   createdAt          DateTime      @default(now())
   updatedAt          DateTime      @updatedAt
@@ -40,17 +45,18 @@
   privateData        String?
 }
 model Leaderboard {
-  id        String   @id @default(uuid())
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-  owner     User     @relation(fields: [ownerId], references: [id])
-  ownerId   String
-  private   Boolean  @default(false)
-  published Boolean  @default(false)
-  players   Player[]
-  title     String
+  id                        String                               @id @default(uuid())
+  createdAt                 DateTime                             @default(now())
+  updatedAt                 DateTime                             @updatedAt
+  owner                     User                                 @relation(fields: [ownerId], references: [id])
+  ownerId                   String
+  private                   Boolean                              @default(false)
+  published                 Boolean                              @default(false)
+  playersScoreSortDirection LeaderboardPlayersScoreSortDirection @default(DESC)
+  players                   Player[]
+  title                     String
   @@index([title])
 }
```


